# Python tooling for book specs, validation, and front-matter generation.
# Authoritative locked versions live in uv.lock (see pyproject.toml).
# Install: `uv sync --frozen` (CI) or `uv sync` / `pip install -r requirements.txt` (dev).
# Lightweight manifest-only: `uv sync --frozen --only-group semantic` (PyYAML + jsonschema).
#
# Floor pin so OpenSSF Scorecard's requirements.txt resolve cannot pick vulnerable
# transitive filelock (<3.20.3). uv.lock already has a newer fixed version.
filelock>=3.32.7
jinja2>=3.1
jsonschema>=4
PyYAML>=6
pytest>=8
ruff>=0.16.2
pre-commit>=4.6.2
python-docx>=1.1
pip-audit>=2.7
