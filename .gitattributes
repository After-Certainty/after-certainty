# Git LFS — chapter narration audio binaries only.
# Receipts and alignment JSON stay in ordinary Git (see docs/roadmaps/elevenlabs-tts-pilot.md).
books/*/audio/*.mp3 filter=lfs diff=lfs merge=lfs -text
