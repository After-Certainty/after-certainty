# OSV / OpenSSF Scorecard exclusions for tools/vercel-cli/package-lock.json.
# Config applies only to the lockfile in this directory (not the root workspace).

[[IgnoredVulns]]
id = "GHSA-fm4j-4xhm-xpwx"
reason = "Advisory targets the abandoned gf3/sandbox npm package (last known affected <1.0.0; sandbox escape via constructors). Locked sandbox@4.x is Vercel's unrelated CLI package (Apache-2.0; depends on @vercel/sandbox). Re-review if the package identity or integrity changes."

[[IgnoredVulns]]
id = "GHSA-gc25-3vc5-2jf9"
reason = "Advisory targets the abandoned gf3/sandbox npm package (all versions flagged; no fix). Locked sandbox@4.x is Vercel's unrelated CLI package (Apache-2.0; depends on @vercel/sandbox). Re-review if the package identity or integrity changes."
