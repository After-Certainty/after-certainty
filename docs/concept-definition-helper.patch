diff --git a/app/explore/(browse)/concepts/[slug]/page.tsx b/app/explore/(browse)/concepts/[slug]/page.tsx
index ef26dea..df06073 100644
--- a/app/explore/(browse)/concepts/[slug]/page.tsx
+++ b/app/explore/(browse)/concepts/[slug]/page.tsx
@@ -17,6 +17,7 @@ import { getConnectedGraphNeighborhood } from "@/lib/graph/graphTraversal";
 import { relatedContentForConcept } from "@/lib/graph/relatedContent";
 import { getExploreSemanticGraph } from "@/lib/explore/exploreSemanticGraph";
 import { createPageMetadata } from "@/lib/metadata";
+import { getConceptDisplayDefinition } from "@/lib/explore/getConceptDisplayDefinition";
 import {
   buildConceptPageJsonLd,
   relatedBookUrls,
@@ -33,7 +34,7 @@ export async function generateMetadata({ params }: PageProps): Promise<Metadata>
   if (!concept) return {};
   return createPageMetadata({
     title: concept.title,
-    description: concept.longDefinition ?? concept.definition ?? concept.shortDefinition,
+    description: getConceptDisplayDefinition(concept, "detail"),
   });
 }
 
@@ -98,7 +99,7 @@ export default async function ExploreConceptDetailPage({ params }: PageProps) {
           {concept.title}
         </h1>
         <div className="mt-8 max-w-2xl space-y-4 text-base leading-[1.85] text-muted md:text-[17px]">
-          <p className="whitespace-pre-wrap">{concept.longDefinition ?? concept.definition ?? concept.shortDefinition}</p>
+          <p className="whitespace-pre-wrap">{getConceptDisplayDefinition(concept, "detail")}</p>
         </div>
         <ExploreEntityDetailActions observatory={{ kind: "concept", slug: concept.slug }} />
         <ExploreAdjacentNav
diff --git a/components/explore/concept-card.tsx b/components/explore/concept-card.tsx
index 7161443..45ed059 100644
--- a/components/explore/concept-card.tsx
+++ b/components/explore/concept-card.tsx
@@ -2,6 +2,7 @@ import Link from "next/link";
 import type { GlossaryConcept } from "@/types/semanticGraph";
 import { ExploreCard } from "@/components/explore/explore-card";
 import { explorePaths } from "@/lib/graph/explorePaths";
+import { getConceptDisplayDefinition } from "@/lib/explore/getConceptDisplayDefinition";
 
 type ConceptCardProps = {
   concept: GlossaryConcept;
@@ -15,7 +16,9 @@ export function ConceptCard({ concept }: ConceptCardProps) {
         <h3 className="font-display text-xl font-medium tracking-tight text-fg transition-colors group-hover:text-accent">
           {concept.title}
         </h3>
-        <p className="line-clamp-3 text-sm leading-relaxed text-muted">{concept.shortDefinition}</p>
+        <p className="line-clamp-3 text-sm leading-relaxed text-muted">
+          {getConceptDisplayDefinition(concept, "card")}
+        </p>
       </Link>
     </ExploreCard>
   );
diff --git a/components/explore/graph-neighborhood-cards.tsx b/components/explore/graph-neighborhood-cards.tsx
index 37b133f..ec8ead4 100644
--- a/components/explore/graph-neighborhood-cards.tsx
+++ b/components/explore/graph-neighborhood-cards.tsx
@@ -2,6 +2,7 @@ import Link from "next/link";
 
 import type { GraphNode } from "@/lib/graph/graph";
 import { exploreHrefForNode } from "@/lib/graph/explorePaths";
+import { getConceptDisplayDefinition } from "@/lib/explore/getConceptDisplayDefinition";
 
 export type GraphNeighborhoodCardsProps = {
   nodes: GraphNode[];
@@ -25,7 +26,9 @@ export function GraphNeighborhoodCards({ nodes, title = "Connected terrain" }: G
               <p className="text-[10px] uppercase tracking-[0.26em] text-accent">{n.kind}</p>
               <p className="mt-2 font-display text-lg text-fg">{n.kind === "source" ? n.entity.name : n.entity.title}</p>
               {n.kind === "concept" ? (
-                <p className="mt-2 line-clamp-2 text-sm text-muted">{n.entity.shortDefinition}</p>
+                <p className="mt-2 line-clamp-2 text-sm text-muted">
+                  {getConceptDisplayDefinition(n.entity, "card")}
+                </p>
               ) : null}
               {n.kind === "pattern" ? (
                 <p className="mt-2 line-clamp-2 text-sm text-muted">{n.entity.summary}</p>
diff --git a/components/explore/observatory/ObservatoryEntityPanel.tsx b/components/explore/observatory/ObservatoryEntityPanel.tsx
index b775553..82283c0 100644
--- a/components/explore/observatory/ObservatoryEntityPanel.tsx
+++ b/components/explore/observatory/ObservatoryEntityPanel.tsx
@@ -9,6 +9,7 @@ import { relatedContentForBook, relatedContentForConcept, relatedContentForPatte
 import { getIncomingRelationships, getOutgoingRelationships, relationshipEndpointsResolved } from "@/lib/graph/graphTraversal";
 import { vizEdgeDedupKey } from "@/lib/graph/graphVizModel";
 import type { Relationship } from "@/types/semanticGraph";
+import { getConceptDisplayDefinition } from "@/lib/explore/getConceptDisplayDefinition";
 
 type ObservatoryEntityPanelProps = {
   index: GraphIndex;
@@ -75,7 +76,9 @@ export function ObservatoryEntityPanel({
           {node.kind === "source" ? node.entity.name : node.entity.title}
         </h2>
         {node.kind === "concept" ? (
-          <p className="mt-4 text-sm leading-relaxed text-muted">{node.entity.shortDefinition}</p>
+          <p className="mt-4 text-sm leading-relaxed text-muted">
+            {getConceptDisplayDefinition(node.entity, "card")}
+          </p>
         ) : null}
         {node.kind === "pattern" ? (
           <p className="mt-4 text-sm leading-relaxed text-muted">{node.entity.summary}</p>
diff --git a/components/explore/observatory/SemanticFlowNode.tsx b/components/explore/observatory/SemanticFlowNode.tsx
index 3fec3ff..2120b42 100644
--- a/components/explore/observatory/SemanticFlowNode.tsx
+++ b/components/explore/observatory/SemanticFlowNode.tsx
@@ -8,6 +8,7 @@ import { Handle, Position, type Node, type NodeProps } from "@xyflow/react";
 import { visualProfileForGraphNode } from "@/lib/graph/nodeVisuals";
 import type { GraphNode } from "@/lib/graph/graph";
 import type { NodeSemanticTier } from "@/lib/observatory/types";
+import { getConceptDisplayDefinition } from "@/lib/explore/getConceptDisplayDefinition";
 
 export type SemanticFlowNodeData = {
   graphNode: GraphNode;
@@ -77,7 +78,7 @@ function NodeTitle({ title, detailHref }: { title: string; detailHref?: string }
 }
 
 function subtitleOf(n: GraphNode): string | undefined {
-  if (n.kind === "concept") return n.entity.shortDefinition;
+  if (n.kind === "concept") return getConceptDisplayDefinition(n.entity, "card");
   if (n.kind === "pattern") return n.entity.summary;
   if (n.kind === "book") return n.entity.subtitle ?? n.entity.summary;
   return n.entity.summary;
diff --git a/components/explore/observatory/panel/EntityDetailView.tsx b/components/explore/observatory/panel/EntityDetailView.tsx
index 01956b4..768f2ee 100644
--- a/components/explore/observatory/panel/EntityDetailView.tsx
+++ b/components/explore/observatory/panel/EntityDetailView.tsx
@@ -15,6 +15,7 @@ import { relationshipEndpointsResolved } from "@/lib/graph/graphTraversal";
 import { relationshipsForConcept } from "@/lib/graph/relationshipTaxonomy";
 import { vizEdgeDedupKey } from "@/lib/graph/graphVizModel";
 import type { Relationship } from "@/types/semanticGraph";
+import { getConceptDisplayDefinition } from "@/lib/explore/getConceptDisplayDefinition";
 
 function labelForId(index: GraphIndex, id: string): string {
   const n = index.getNodeByCanonicalId(id);
@@ -99,7 +100,9 @@ export function EntityDetailView({
           {node.kind === "source" ? node.entity.name : node.entity.title}
         </h2>
         {node.kind === "concept" ? (
-          <p className="mt-4 text-sm leading-relaxed text-muted">{node.entity.shortDefinition}</p>
+          <p className="mt-4 text-sm leading-relaxed text-muted">
+            {getConceptDisplayDefinition(node.entity, "card")}
+          </p>
         ) : null}
         {node.kind === "pattern" ? (
           <p className="mt-4 text-sm leading-relaxed text-muted">{node.entity.summary}</p>
diff --git a/lib/explore/getConceptDisplayDefinition.ts b/lib/explore/getConceptDisplayDefinition.ts
new file mode 100644
index 0000000..ed06d61
--- /dev/null
+++ b/lib/explore/getConceptDisplayDefinition.ts
@@ -0,0 +1,30 @@
+import type { GlossaryConcept } from "@/types/semanticGraph";
+
+/**
+ * Get the appropriate definition text for displaying a concept in different contexts.
+ *
+ * This centralizes the field selection logic to prevent drift between
+ * concept index cards, observatory cards, search results, and related terrain.
+ *
+ * @param concept - The concept to get a definition for
+ * @param variant - The display context
+ * @returns The appropriate definition text
+ */
+export function getConceptDisplayDefinition(
+  concept: GlossaryConcept,
+  variant: "index" | "detail" | "card" = "card"
+): string {
+  switch (variant) {
+    case "detail":
+      // Detail pages prefer richer longDefinition, with fallback chain
+      return (
+        concept.longDefinition ?? concept.definition ?? concept.shortDefinition
+      );
+
+    case "index":
+    case "card":
+    default:
+      // Index and cards always use the concise, portable shortDefinition
+      return concept.shortDefinition;
+  }
+}
