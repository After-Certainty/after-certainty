# Logical chapter-TTS voice aliases → provider-native voice settings.
# Authored units reference `voice: <alias>` (not raw provider IDs).
# Replace PLACEHOLDER_* before a real generate run (AUDIO-P0-03).

voices:
  reflective-narrator:
    elevenlabs:
      voice_id: PLACEHOLDER_ELEVENLABS_VOICE_ID
    openai:
      voice: cedar
      instructions: >
        Read in a calm, reflective, restrained narrative style.
